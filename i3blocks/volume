#!/bin/bash

# Check for clicks
if [ -n "${BLOCK_BUTTON}" ]
then
	vol --notify &> /dev/null
fi

# Get volume level
vol=$(printf "%-4s" "$(vol -S)")

echo "  <span font='monospace'>${vol}</span>"
echo "  <span font='monospace'>${vol}</span>"
echo 
